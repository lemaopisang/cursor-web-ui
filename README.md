# how to run

clone this repo ofc
go to terminal and do these commands:

```powershell
npm install
npm run dev
npm run build
npm run preview and go the localhost address that u got

tell me if bug lol
