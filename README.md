# how to run

clone this repo ofc
go to terminal and do these commands:

```powershell
npm install
npm run dev
npm run build
npm run preview
```

then u enter the adress that u got
should be done after dat :)
